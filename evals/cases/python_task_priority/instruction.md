请为这个已有的任务清单程序增加优先级功能，同时保持旧数据兼容：

1. 每个 `Task` 增加字符串优先级 `low`、`normal` 或 `high`，默认值为 `normal`；其他值必须
   被拒绝。
2. JSON 存储必须写入优先级；读取不含该字段的旧 JSON 时按 `normal` 处理。
3. `add` 子命令增加可选参数 `--priority low|normal|high`，不传时使用 `normal`。
4. `list` 按 `high`、`normal`、`low` 的顺序显示；相同优先级保持原有顺序。每行格式为
   `[HIGH] [ ] title`，已完成任务仍使用 `[x]`。

不要破坏现有命令和公开测试。请先阅读项目结构，完成必要的多文件修改，并运行测试及适当的
命令行示例验证结果。
